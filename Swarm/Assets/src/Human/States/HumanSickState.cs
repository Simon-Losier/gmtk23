using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Human is sick(a zombie)
public class HumanSickState : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
